version: "1"

capabilities:
  - contract_id: "abesh:httpserver"
    values:
      host: "0.0.0.0"
      port: "9090"

services:
  - contract_id: "abesh:ex_echo"
    authorizer:
      contract_id: "TODO"
      operator: "AND"
      rules:
        - "TODO"
    values:
      key1: "test1"
      key2: "test2"

    triggers:
      - contract_id: "abesh:httpserver"
        trigger_values:
          method: "GET"
          path: "/default"

      - contract_id: "abesh:httpserver"
        trigger_values:
          method: "GET"
          path: "/echo"
